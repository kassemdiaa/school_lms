import 'package:flutter/material.dart';

class OverviewTap extends StatelessWidget {
  const OverviewTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(color: Colors.red,);
  }
}