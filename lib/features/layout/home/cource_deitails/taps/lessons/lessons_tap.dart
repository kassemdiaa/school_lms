import 'package:flutter/material.dart';

class LessonsTap extends StatelessWidget {
  const LessonsTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}