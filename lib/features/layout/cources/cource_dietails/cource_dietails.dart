import 'package:flutter/material.dart';

class CourceDietails extends StatelessWidget {
  const CourceDietails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}