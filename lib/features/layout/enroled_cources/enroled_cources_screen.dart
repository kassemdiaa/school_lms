import 'package:flutter/material.dart';

class EnroledCourcesScreen extends StatelessWidget {
  const EnroledCourcesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(color: Colors.blue,);
  }
}